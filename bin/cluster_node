#!/usr/bin/env iojs

/* jslint node: true, esnext: true */

"use strict";

const csm = require('consul-service-manager');


csm.manager().then(function (manager) {
	console.log(`manager: ${manager}`);
}, function (err) {
	console.log(`manager failed: ${err}`);
});
